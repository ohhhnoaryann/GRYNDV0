# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/grynd_db

# PostgreSQL Connection Details (if using separate connection)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=grynd_db

# Node.js Environment
NODE_ENV=production
PORT=5000